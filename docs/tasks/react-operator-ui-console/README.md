# React Operator UI Console

Date: 2026-04-25

This module defines the React Operator UI implementation plan for `myclaw`.

The goal is to expose the runtime capabilities already implemented in Go through a rich web console based on `myclawd` and Ant Design X.

Read `task.md` first.
