# P1 AppState Session Continuity

Runtime-owned implementation task for P1.1 app state and session continuity.

This task makes session, runtime, approval, task/subagent, and client-visible state recoverable as one continuation model.

Read `task.md` first.
