# P1 Context Cache Memory Depth

Runtime-owned implementation task for P1.2 context cache and memory depth.

This task makes workspace instructions, read-file state, context cache, projected history, history replay, and compaction memory recovery deterministic across restart and rebuild.

Read `task.md` first.
